# ─── PRODUCCIÓN ───────────────────────────────────────────────────────────────
# Copia estas variables en Railway / Render / tu servidor

# Base de datos (Railway provee automáticamente DATABASE_URL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Redis (Railway provee automáticamente REDIS_URL)
REDIS_URL="redis://HOST:PORT"

# JWT — CAMBIA ESTOS POR VALORES SEGUROS EN PRODUCCIÓN
JWT_SECRET="GENERA-UN-SECRET-SEGURO-MIN-32-CHARS"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="GENERA-OTRO-SECRET-SEGURO-MIN-32-CHARS"
JWT_REFRESH_EXPIRES_IN="7d"

# Puerto (Railway lo asigna automáticamente)
PORT=3000

# Frontend URL (para CORS)
FRONTEND_URL="https://tu-app.vercel.app"
